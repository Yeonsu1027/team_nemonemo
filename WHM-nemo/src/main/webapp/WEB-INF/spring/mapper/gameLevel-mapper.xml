<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.whm0304.nemo.persistance.GameLevelDao">
	<select id="selectAll">
		SELECT *
		FROM tbl_nemo
		WHERE n_num = #{n_num}
	</select>

</mapper>